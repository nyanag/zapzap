{
    "name": "milestone-2-tmp",
    "version": "1.0.0",
    "description": "",
    "main": "index-firebase.js",
    "scripts": {
        "start": "nodemon index.js",
        "test": "NODE_ENV=test jasmine --reporter=jasmine-console-reporter",
        "test-db": "NODE_ENV=test jasmine --reporter=jasmine-console-reporter spec/database.spec.js",
        "test-login": "NODE_ENV=test jasmine --reporter=jasmine-console-reporter spec/login.spec.js",
        "test-classes": "NODE_ENV=test jasmine --reporter=jasmine-console-reporter spec/class-endpoints.spec.js",
        "test-session-codes": "NODE_ENV=test jasmine --reporter=jasmine-console-reporter spec/class-session-code-endpoints.spec.js",
        "test-class-sessions": "NODE_ENV=test jasmine --reporter=jasmine-console-reporter spec/class-session-endpoints.spec.js",
        "lint-report": "eslint --output-file eslint-report.json --format json src/",
        "lint-terminal": "eslint src/",
        "lint": "npm run lint-report; npm run lint-terminal;"
    },
    "type": "module",
    "author": "",
    "license": "ISC",
    "engines": {
        "node": ">=16"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "csv-parser": "^3.0.0",
        "express": "^4.18.2",
        "multer": "^1.4.5-lts.1",
        "nodemailer": "^6.9.3",
        "nodemon": "^2.0.20"
    },
    "devDependencies": {
        "eslint": "^8.35.0"
    }
}
